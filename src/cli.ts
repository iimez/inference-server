#!/usr/bin/env node
process.stdout.write("Nothing here, yet.\n");

/* API Ideas:
- `infs ls` - list stored models
- `infs rm <name>` - remove a stored model
- `infs show <name>` - show details of a stored model
- `infs prepare <config>` - download everything required for given config
*/